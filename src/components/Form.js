import React, { Component } from 'react';

class Form extends Component {
  render() {
    return (
      <div>
        <h1>Add a new type of boiler...</h1>
      </div>
    );
  }
}

export default Form;
